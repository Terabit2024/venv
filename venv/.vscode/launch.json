{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Streamlit: Run",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/page.py",
            "console": "integratedTerminal",
            "args": [
                "run"
            ],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            }
        }
    ]
}